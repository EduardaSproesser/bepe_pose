# pose_estimation

Repositório local criado a partir da pasta `pose_estimation`.

Descrição

Projeto para estimativa de pose (placeholder). Adicione seus arquivos fonte aqui.

Como usar

1. Ativar o ambiente virtual (PowerShell):

   ```powershell
   .\.venv\Scripts\Activate.ps1
   ```

   Ou no cmd:

   ```cmd
   .\.venv\Scripts\activate.bat
   ```

2. Instalar dependências (se houver `requirements.txt`):

   ```powershell
   pip install -r requirements.txt
   ```

3. Rodar scripts com o Python do venv:

   ```powershell
   .\.venv\Scripts\python.exe seu_script.py
   ```

Observações

- Este README.md foi criado automaticamente. Atualize-o conforme o projeto evolui.
